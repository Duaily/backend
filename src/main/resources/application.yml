spring:
  datasource:
    url: jdbc:mysql://mysql:3306/test_db?useSSL=false&serverTimezone=Asia/Seoul
    username: test
    password: test_pw
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql   # 추가 해준 부분
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect # 추가 해준 부분
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
    host: redis
    port: 6379
  profiles:
    include:
      secret
logging:
  level:
    com:
      amazonaws:
        util:
          EC2MetadataUtils: ERROR